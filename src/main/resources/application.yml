server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/toggl
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      enable_lazy_load_no_trans: true
